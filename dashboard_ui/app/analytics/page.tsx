
/**
 * Should include:
 * - Pipeline metrics
 * - scikit-learn metrics
 * - Clinic metrics (number replied, queued etc)
 * - Db metrics (scores etc)
 * - AI (time it takes to generate a response/ average per batch)
 */
export default function Analytics() {

  return (
    <div>
      <h1></h1>
      Analytics
    </div>
  )
}
