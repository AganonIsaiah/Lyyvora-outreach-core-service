
/**
 * TODO: low prio
 * Should
 * - Allow user to configure prompt + (shows default prompt)
 * - Allow users to 
 */
export default function Configuration() {


  return (
    <div>

    </div>
  )
}